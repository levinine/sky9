default: &default
  REGION: eu-west-2
  ACCOUNT_TABLE: Account-${self:provider.stage}
local:
  <<: *default
  DB_ENDPOINT: http://localhost:8000
  API_ENDPOINT: http://localhost:4000
dev:
  <<: *default
  DB_ENDPOINT: dynamodb.eu-west-2.amazonaws.com
test:
  <<: *default
  
  
